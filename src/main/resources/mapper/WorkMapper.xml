<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feng.militia_admin.mapper.WorkMapper">

    <resultMap id="BaseResultMap" type="com.feng.militia_admin.model.domain.Work">
        <id property="id" column="id" />
        <result property="title" column="title" />
        <result property="type" column="type" />
        <result property="orgId" column="org_id" />
        <result property="targetOrgIds" column="target_org_ids" />
        <result property="status" column="status" />
        <result property="createUserId" column="create_user_id" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="isDelete" column="is_delete" />
    </resultMap>

    <sql id="Base_Column_List">
        w.id,
        w.title,
        w.type,
        w.org_id AS orgId,
        o.name AS orgName,
        w.target_org_ids AS targetOrgIds,
        w.status,
        u.name AS createUserName,
        w.create_time AS createTime
    </sql>

    <!-- 军长查看自己发布的工作列表 -->
    <select id="selectWorkListByOrgId" resultType="com.feng.militia_admin.model.vo.ShowWorkInfoVo">
        SELECT
        <include refid="Base_Column_List" />
        FROM `work` w
        INNER JOIN organization o ON w.org_id = o.id
        LEFT JOIN `user` u ON w.create_user_id = u.id
        WHERE w.org_id = #{orgId}
        AND w.is_delete = 0
        AND o.is_delete = 0
        ORDER BY w.status ASC, w.create_time DESC
    </select>

</mapper>
